{
  "extends": "./tsconfig.json",
  "include": ["src/**/*.ts", "tests/**/*.ts", "src/types/*.d.ts"],
  "exclude": ["node_modules", "dist"],
  "compilerOptions": {
    "composite": true,
    "types": ["jest", "node"]
  }
}
